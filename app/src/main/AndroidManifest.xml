<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.root.deep"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".deeplink.DispatcherActivity"
            android:allowTaskReparenting="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="dianrong" />
            </intent-filter>
        </activity>
        <activity android:name=".DestActivity">
        </activity>
        <activity android:name=".deeplink.WebViewActivity">
        </activity>
        <activity android:name=".jsbridge.BridgeWebActivity">
        </activity>
        <activity android:name=".jsroad.RoadWebActivity" />
    </application>

</manifest>